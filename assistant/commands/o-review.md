---
description: "[Organize] 每日回顾 - 分发 + 复盘 + 规划"
argument-hint: "[YYYY-MM-DD] [--auto]"
---

插件根目录：${CLAUDE_PLUGIN_ROOT}（作为 ASSISTANT_PLUGIN_ROOT 传给 skill 中的脚本调用）

执行 `o-review` skill，透传 `$ARGUMENTS`；由当前宿主提供活动统计。
