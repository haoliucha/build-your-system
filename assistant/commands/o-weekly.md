---
description: "[Organize] 每周整合 - 扫描本周内容，生成周报"
argument-hint: "[--bootstrap-digest]"
---

插件根目录：${CLAUDE_PLUGIN_ROOT}（作为 ASSISTANT_PLUGIN_ROOT 传给 skill 中的脚本调用）

执行 `o-weekly` skill，透传 `$ARGUMENTS`。
