---
description: "一键口径与红线速查:读 memory+生成器源,出锁定口径表(对客/内部分层)+『勿口播』红线清单+遗留待办三清单(待实测/待确认/待核)+关键数字漂移抽查,全程只读。详见 skill bid-playbook"
argument-hint: ""
---

# /bid:status

参数：`$ARGUMENTS`

加载本插件的 `bid-status` skill，把 `$ARGUMENTS` 作为本次输入透传，并完整执行该 skill。命令文件只负责 Claude Code 入口；流程、护栏和停止条件以 skill 为唯一真源。
